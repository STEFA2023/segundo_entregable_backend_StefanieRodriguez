<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listado de Productos</title>
</head>
<body>
    <h1>Listado de Productos</h1>
    <ul>
        {{#each products}}
        <li><img src="{{thumbnail}}" alt="{{title}}"><br>
            <strong>{{title}}</strong><br>
            <em>{{description}}</em><br>
            ${{price}}</li>
        {{/each}}
    </ul>
</body>
</html>
