<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Listado de Usuarios</title>
</head>
<body>
    <h1>Listado de Usuarios</h1>
    <ul>
        {{#each users}}
        <li>{{lastName}}, {{firstName}} {{#if active}}(activo){{/if}}</li>
        {{/each}}
    </ul>
</body>
</html>
